// netlify/functions/api/verify-2fa.js
const speakeasy = require("speakeasy");
const jwt = require("jsonwebtoken");

exports.handler = async (event) => {
  try {
    const { code } = JSON.parse(event.body || "{}");

    if (!code) {
      console.error("❌ Aucun code 2FA reçu");
      return {
        statusCode: 400,
        body: JSON.stringify({ error: "Missing 2FA code" })
      };
    }

    const secret = process.env.TOTP_SECRET;
    const jwtSecret = process.env.JWT_SECRET;

    if (!secret || !jwtSecret) {
      console.error("❌ Variables d'environnement manquantes:", { secret, jwtSecret });
      return {
        statusCode: 500,
        body: JSON.stringify({ error: "Missing environment variables" })
      };
    }

    // 🔎 Récupération du cookie JWT
    const cookies = event.headers.cookie || "";
    const sessionCookie = cookies.split(";").find(c => c.trim().startsWith("session="));
    if (!sessionCookie) {
      console.error("❌ Aucun cookie de session trouvé");
      return {
        statusCode: 401,
        body: JSON.stringify({ error: "No session cookie found" })
      };
    }

    const token = sessionCookie.split("=")[1];
    let payload;
    try {
      payload = jwt.verify(token, jwtSecret);
    } catch (err) {
      console.error("❌ JWT invalide:", err.message);
      return {
        statusCode: 401,
        body: JSON.stringify({ error: "Invalid session token" })
      };
    }

    // ✅ Vérification du code TOTP
    const verified = speakeasy.totp.verify({
      secret,
      encoding: "base32",
      token: code,
      window: 1 // tolérance ±30s
    });

    console.log("🔐 Code 2FA reçu:", code);
    console.log("✅ Vérification:", verified);

    if (!verified) {
      return {
        statusCode: 401,
        body: JSON.stringify({ error: "Invalid 2FA code" })
      };
    }

    // 🔑 Mise à jour du JWT avec twoFA:true
    const newToken = jwt.sign(
      {
        email: payload.email,
        googleId: payload.googleId,
        twoFA: true
      },
      jwtSecret,
      { expiresIn: "15m" }
    );

    // ✅ Redirection vers /admin avec nouveau cookie
    return {
      statusCode: 302,
      headers: {
        "Set-Cookie": `session=${newToken}; HttpOnly; Secure; Path=/; SameSite=Lax`,
        Location: "/admin"
      }
    };

  } catch (err) {
    console.error("❌ Erreur dans verify-2fa.js:", err);
    return {
      statusCode: 500,
      body: JSON.stringify({ error: "Internal Server Error", details: err.message })
    };
  }
};

